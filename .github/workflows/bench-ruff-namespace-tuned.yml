name: bench-ruff-namespace-tuned
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: namespace-profile-asgar
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --release
